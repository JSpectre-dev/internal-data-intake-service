# Internal Data Intake Service

## Overview
This project is a backend service that ingests structured data files, validates records, and stores clean and rejected data separately.

## Current Status
- FastAPI application initialized
- Health check endpoint available

More details to be added as features are implemented.